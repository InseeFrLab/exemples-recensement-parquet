```{{ojs}}
Inputs.table(
  db.query(
    "SELECT * FROM documentation WHERE CONTAINS(code_modalite, 'Z')"
  )
)
```

```{ojs}
//| echo: false
Inputs.table(
  db.query(
    "SELECT * FROM documentation WHERE CONTAINS(code_modalite, 'Z')"
  )
)
```

